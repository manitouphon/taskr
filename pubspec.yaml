name: taskr
description: Make task and todos on this app

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 6.9.420

environment:
  sdk: ">=2.7.0 <3.0.0"
  

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  google_fonts: ^1.1.0


  sqflite:
  path:
  

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - assets/images/